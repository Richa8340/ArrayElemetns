function printArrayElements(array) {
    array.forEach(element => {
        console.log(element);
    });
}

// Usage example
printArrayElements([1, 2, 3, 4, 5]); // Logs 1, 2, 3, 4, 5 each on a new line
